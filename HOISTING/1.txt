Hoisting is JavaScript's behavior of allocating memory for variable and function declarations before the code is executed. During the Memory Creation Phase, the engine scans the code. Standard function declarations are fully hoisted, meaning they can be called before they appear in the code. Variables are also hoisted, but var is initialized as undefined, while let and const are placed in the Temporal Dead Zone and will throw an error if accessed early.